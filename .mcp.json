{
  "mcpServers": {
    "hot-reload": {
      "command": "npx",
      "args": [
        "mcp-reloader",
        "--include", "config/**/*.json",
        "--include", "src/lib/**/*.js"
      ],
      "env": {
        "NODE_ENV": "development"
      }
    },
    "hot-reload-custom": {
      "command": "npx",
      "args": [
        "mcp-reloader",
        "--include", "**/*.yaml",
        "--",
        "python",
        "my-lsp-server.py",
        "--port", "3000"
      ]
    },
    "hot-reload-complex": {
      "command": "npx",
      "args": [
        "mcp-reloader",
        "--include", "config/**/*.{json,yaml}",
        "--include", "src/**/*.ts",
        "--",
        "node",
        "--experimental-specifier-resolution=node",
        "./dist/server.js",
        "--config", "./config/server.json",
        "--log-level", "debug"
      ]
    }
  }
}